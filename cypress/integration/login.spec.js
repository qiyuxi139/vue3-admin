/* eslint-env mocha */
describe("Login test", () => {
  it("ite", () => {
    // cy.visit("/login");
    // cy.get(".loginForm > div:nth-of-type(1) .inputItem input").type("admin");
    // cy.get(".loginBtn").click();
    // cy.get(".is-error").should("contain", "密码不能为空");
    // cy.get(".loginForm > div:nth-of-type(2) .inputItem input").type("123");
    // cy.intercept("/api/user/login", {
    //   code: 200,
    //   message: "success",
    //   result: true
    // });
    // cy.get(".loginBtn").click();
    // const obj = { name: "Jane", age: 18, obj: { a: 1, b: 2 } };
    // expect(obj).to.have.any.keys("age");
    // expect({ a: { b: ["x", "y"] } }).to.have.nested.property("a.b[0]");
    // expect({ a: { b: ["x", "y"] } }).to.nested.include({ "a.b[1]": "y" });
    // expect(false).to.be.false;
    assert.isTrue(true, "this val is true");
  });
});
